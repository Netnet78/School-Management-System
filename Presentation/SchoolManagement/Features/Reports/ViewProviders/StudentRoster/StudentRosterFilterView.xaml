<UserControl x:Class="SchoolManagement.Presentation.Features.Reports.ViewProviders.StudentRoster.StudentRosterFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SchoolManagement.Presentation.Features.Reports.ViewProviders.StudentRoster"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:StudentRosterFilterViewModel}"
             d:DesignHeight="80" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Horizontal" Margin="0 0 15 0">
            <TextBlock Text="ថ្នាក់" VerticalAlignment="Center" Margin="0 0 5 0"/>
            <ComboBox Width="150"
                      ItemsSource="{Binding Classes}"
                      SelectedItem="{Binding SelectedClass}"
                      DisplayMemberPath="KhmerName"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0 0 15 0">
            <TextBlock Text="កម្រិត" VerticalAlignment="Center" Margin="0 0 5 0"/>
            <ComboBox Width="120"
                      ItemsSource="{Binding Grades}"
                      SelectedItem="{Binding SelectedGrade}"
                      DisplayMemberPath="KhmerName"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0 0 15 0">
            <TextBlock Text="ជំនាញ" VerticalAlignment="Center" Margin="0 0 5 0"/>
            <ComboBox Width="120"
                      ItemsSource="{Binding Skills}"
                      SelectedItem="{Binding SelectedSkill}"
                      DisplayMemberPath="KhmerName"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <CheckBox IsChecked="{Binding IncludeInactive}"
                      Content="បង្ហាញសិស្សឈប់រៀន"
                      VerticalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</UserControl>
