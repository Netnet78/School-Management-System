<UserControl x:Class="SchoolManagement.Presentation.Features.Reports.ViewProviders.StudentCard.StudentCardOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SchoolManagement.Presentation.Features.Reports.ViewProviders.StudentCard"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance Type=local:StudentCardOptionsViewModel}"
             d:DesignHeight="200" d:DesignWidth="600"
             TextElement.FontSize="16">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ព័ត៌មានទីតាំង" VerticalAlignment="Center" Margin="0 0 12 16"/>
        <Grid Grid.Row="0" Grid.Column="1" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Padding="8" FontFamily="{StaticResource NotoSansKhmer}"
                     Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Grid.Column="1" Text="នាយក" VerticalAlignment="Center" Margin="15 0 10 0"/>
            <TextBox Grid.Column="2" Padding="8" FontFamily="{StaticResource NotoSansKhmer}"
                     Text="{Binding PrincipalName, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <TextBlock Grid.Row="1" Text="ហត្ថាលេខានាយក" VerticalAlignment="Center" Margin="0 0 12 16"/>
        <Grid Grid.Row="1" Grid.Column="1" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Padding="8" FontFamily="{StaticResource NotoSansKhmer}"
                     Text="{Binding SignaturePath, UpdateSourceTrigger=LostFocus}"
                     IsReadOnly="True"
                     Margin="0 0 10 0"/>
            <Button Grid.Column="1" Command="{Binding BrowseSignatureCommand}"
                    Content="Browse" Padding="12 8" FontSize="16"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="កាលបរិច្ឆេទផលិត" VerticalAlignment="Center" Margin="0 0 12 0"/>
        <DatePicker Padding="8" 
                    Grid.Row="2" Grid.Column="1"
                    FontFamily="{StaticResource NotoSansKhmer}"
                    SelectedDate="{Binding CreatedDate}"
                    Margin="0 0 10 12"/>
    </Grid>
</UserControl>
