<UserControl x:Class="SchoolManagement.Presentation.Features.Students.Views.AddStudentOptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SchoolManagement.Presentation.Features.Students.Views" 
             xmlns:viewmodels="clr-namespace:SchoolManagement.Presentation.Features.Students.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:AddStudentOptionViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="1200">

    <UserControl.Resources>

        <!-- Primary Card Button -->
        <Style x:Key="PrimaryCardButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="350"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0 0 20 0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary}"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary.Foreground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border"
                                        Property="Opacity"
                                        Value="0.92"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border"
                                        Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98"
                                                        ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Card Button -->
        <Style x:Key="SecondaryCardButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource PrimaryCardButtonStyle}">
            <Setter Property="Width" Value="340"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Secondary.Foreground}"/>
        </Style>

    </UserControl.Resources>

    <Border Background="{DynamicResource MaterialDesign.Brush.Background}">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <Border Width="64"
                    Height="64"
                    CornerRadius="100"
                    Background="{DynamicResource MaterialDesign.Brush.Primary}"
                    Margin="0 0 0 10">

                <materialDesign:PackIcon Kind="UserAddOutline"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"
                                         Width="32"
                                         Height="32"
                                         Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}"/>
            </Border>

            <TextBlock Text="សូមធ្វើការជ្រើសរើសជម្រើសមួយខាងក្រោម"
                       TextAlignment="Center"
                       FontSize="24"
                       Margin="0 0 0 20"/>

            <StackPanel Orientation="Horizontal">

                <!-- Option 1 -->
                <Button Style="{StaticResource PrimaryCardButtonStyle}"
                        Command="{Binding GoToAssignCandidateViewCommand}">
                    <StackPanel>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    Margin="0 0 0 10">

                            <materialDesign:PackIcon Kind="TicketUser"
                                                     Width="36"
                                                     Height="36"
                                                     VerticalAlignment="Center"/>

                            <TextBlock Text="ច្រោះយក​បេក្ខជននិងដាក់ងារ​ជាសិស្ស"
                                       Margin="6 0 0 0"
                                       FontSize="20"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <Rectangle Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                   Height="1"/>

                        <StackPanel Margin="0 5 0 0">

                            <TextBlock FontSize="16"
                                Text="ជ្រើសរើសជម្រើសនេះប្រសិនបើអ្នកត្រូវការយកឈ្មោះ សិស្សដែលបានចុះឈ្មោះណាម្នាក់ដាក់ងារជា
                                       សិស្សានុសិស្សដោយមិនចាំបាច់សរសេរព័ត៌មាន
                                       អ្វីបន្ថែមឡើយ"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                    </StackPanel>
                </Button>

                <!-- Option 2 -->
                <Button Style="{StaticResource SecondaryCardButtonStyle}"
                        Command="{Binding GoToAddStudentViewCommand}">
                    <StackPanel>

                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Margin="0 0 0 10">

                            <materialDesign:PackIcon Kind="UserAdd"
                                                     Width="36"
                                                     Height="36"/>

                            <TextBlock Text="បន្ថែមសិស្សថ្មី"
                                       Margin="6 0 0 0"
                                       FontSize="20"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <Rectangle Height="1"
                                   Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>

                        <StackPanel Margin="0 5 0 0">

                            <TextBlock FontSize="16"
                                Text="ជ្រើសរើសជម្រើសនេះប្រសិនបើអ្នកត្រូវការបន្ថែមសិស្សថ្មីទាំងស្រុង"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                    </StackPanel>
                </Button>

            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>