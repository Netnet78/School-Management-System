<UserControl x:Class="New_Student_Management.Views.MainFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:New_Student_Management.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance Type=viewModels:MainFormViewModel}"
             mc:Ignorable="d">
    <Grid Background="{DynamicResource MaterialDesign.Brush.Background}">
        <Border Margin="24"
                Padding="24"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{DynamicResource MaterialDesign.Brush.Primary}">
            <StackPanel>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Margin="0 0 10 0"
                                         Width="48"
                                         Height="48"
                                         Kind="HomeCircle"
                                         HorizontalAlignment="Left"
                                         Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                                         VerticalAlignment="Center"/>
                        <TextBlock FontSize="28"
                           FontWeight="SemiBold"
                           Text="សូមស្វាគមន៍!" 
                           VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock FontSize="18"
                           TextWrapping="Wrap"
                           Margin="0 10 0 0"
                           Text="{Binding WelcomeMessage}" />
                    <TextBlock Margin="0 18 0 0"
                           FontSize="14"
                           Opacity="0.4"
                           FontWeight="Bold"
                           Text="កម្មវិធីត្រូវបានផលិតឡើងដោយ៖ សិស្សានុសិស្សផ្នែកកុំព្យូទ័រឆ្នាំទី៣ ជំនាន់ទី១៤" />
                </StackPanel>

                <StackPanel Margin="0 15 0 0">
                    
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
