<UserControl x:Class="CandidateManagement.Views.InsertStudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CandidateManagement.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:SchoolManagement.Presentation.Shared.XAMLConverters;assembly=SchoolManagement.Presentation.Shared"
             xmlns:viewmodels="clr-namespace:CandidateManagement.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:InsertStudentViewModel}"
             Background="{DynamicResource MaterialDesign.Brush.Background}">

    <UserControl.Resources>
        <conv:IntEqualsToVisibilityConverter x:Key="IntEqualsToVisibilityConverter" />
        <conv:DateOnlyToDateTimeConverter x:Key="DateOnlyToDateTimeConverter" />
        <conv:PhotoPathToImageSourceConverter x:Key="PhotoPathToImageSourceConverter" />
        <conv:ValidPathToVisibilityConverter x:Key="ValidPathToVisibilityConverter" Reversed="False" />
        <conv:ValidPathToVisibilityConverter x:Key="ValidPathToVisibilityConverterReversed" Reversed="True"/>
    </UserControl.Resources>

    <UserControl.InputBindings>
        <KeyBinding Key="E" Modifiers="Ctrl" Command="{Binding NextCommand}" />
        <KeyBinding Key="Q" Modifiers="Ctrl" Command="{Binding BackCommand}" />
        <KeyBinding Key="Enter" Command="{Binding AddStudentCommand}" />
    </UserControl.InputBindings>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource MaterialDesign.Brush.Primary}" CornerRadius="8" Padding="16" Margin="0 0 0 16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="AccountPlus" Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}" Width="48" Height="48" Margin="0 0 16 0"/>
                <TextBlock Text="ទម្រង់បញ្ចូលសិស្សានុសិស្សថ្មី" 
                           Foreground="{DynamicResource MaterialDesign.Brush.Primary.Foreground}" 
                           FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Form Content in a Card -->
        <materialDesign:Card Grid.Row="1" Padding="24" UniformCornerRadius="8" TextElement.FontSize="16" TextElement.FontFamily="{StaticResource NotoSansKhmer}">
            <Grid>
                <!-- Step 0: Personal -->
                <ScrollViewer Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=0}" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="ព័ត៌មានផ្ទាល់ខ្លួន" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBox Text="{Binding Data.LastName, Mode=TwoWay}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="គោត្តនាម" Margin="0,8"/>
                                <TextBox Text="{Binding Data.LatinLastName, Mode=TwoWay}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="គោត្តនាមជាអក្សរឡាតាំង" Margin="0,8"/>
                                <TextBox Text="{Binding Data.FirstName, Mode=TwoWay}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="នាម" Margin="0,8"/>
                                <TextBox Text="{Binding Data.LatinFirstName, Mode=TwoWay}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="នាមជាអក្សរឡាតាំង" Margin="0,8"/>
                                
                                <ComboBox SelectedValue="{Binding Data.Gender}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="ភេទ" IsEditable="False" DisplayMemberPath="Description" SelectedValuePath="Value" ItemsSource="{Binding GenderOptions}" Margin="0,8"/>
                                
                                <DatePicker SelectedDate="{Binding Data.DateOfBirth, Converter={StaticResource DateOnlyToDateTimeConverter}}" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" materialDesign:HintAssist.Hint="កាលបរិច្ឆេទកំណើត" Margin="0,8"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <ComboBox SelectedValue="{Binding Data.SkillId}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="ជំនាញ" DisplayMemberPath="KhmerName" SelectedValuePath="Id" IsEditable="False" ItemsSource="{Binding SkillOptions}" Margin="0,8"/>
                                <TextBox Text="{Binding Data.Religion}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="សាសនា" Margin="0,8"/>
                                <TextBox Text="{Binding Data.SiblingsCount}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ចំនួនបងប្អូន (រាប់ទាំងសមីខ្លួន)" Margin="0,8"/>
                                <TextBox Text="{Binding Data.PhoneNumber}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="លេខទូរស័ព្ទ" Margin="0,8"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Step 1: Birth location -->
                <ScrollViewer Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=1}" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="ទីកន្លែងកំណើត" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBox Text="{Binding Data.BirthVillage}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ភូមិ" Margin="0,8"/>
                                <TextBox Text="{Binding Data.BirthCommune}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ឃុំ/សង្កាត់" Margin="0,8"/>
                                <TextBox Text="{Binding Data.BirthDistrict}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ស្រុក/ក្រុង" Margin="0,8"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBox Text="{Binding Data.BirthProvince}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ខេត្ត" Margin="0,8"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Step 2: Father / Mother info -->
                <ScrollViewer Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=2}" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="ឪពុក និង ម្ដាយ" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBox Text="{Binding Data.MotherName}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ឈ្មោះម្ដាយ" Margin="0,8"/>
                                <TextBox Text="{Binding Data.MotherOccupation}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="មុខរបរម្ដាយ" Margin="0,8"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBox Text="{Binding Data.FatherName}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ឈ្មោះឪពុក" Margin="0,8"/>
                                <TextBox Text="{Binding Data.FatherOccupation}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="មុខរបរឪពុក" Margin="0,8"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Step 3: Exam & Other -->
                <ScrollViewer Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=3}" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="ព័ត៌មានប្រឡង និង ផ្សេងៗ" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBox Text="{Binding Data.ExamCenter}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="មណ្ឌលប្រឡង" Margin="0,8"/>
                                <DatePicker SelectedDate="{Binding Data.ExamDate, Converter={StaticResource DateOnlyToDateTimeConverter}}" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" materialDesign:HintAssist.Hint="កាលបរិច្ឆេទប្រឡង" Margin="0,8"/>
                                <TextBox Text="{Binding Data.ExamTable}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="លេខតុ" Margin="0,8"/>
                                <TextBox Text="{Binding Data.ExamRoom}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="លេខបន្ទប់" Margin="0,8"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBox Text="{Binding Data.FromSchool}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="មកពីសាលារៀន" Margin="0,8"/>
                                <ComboBox ItemsSource="{Binding StayTypeOptions}" DisplayMemberPath="Description" SelectedValuePath="Value" SelectedValue="{Binding Data.StayType}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="ការស្នាក់នៅ" IsEditable="False" Margin="0,8"/>
                                <TextBox Text="{Binding Data.OtherInfo}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ផ្សេងៗ" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="80" Margin="0,8"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Step 4: Picture -->
                <ScrollViewer Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=4}" VerticalScrollBarVisibility="Auto">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="ព័ត៌មានរូបភាព" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" Margin="0,0,0,24" HorizontalAlignment="Center"/>
                        
                        <materialDesign:Card Width="240" Height="300" Padding="8" UniformCornerRadius="8" Background="{DynamicResource MaterialDesign.Brush.Surface}">
                            <Grid>
                                <Image Stretch="UniformToFill"
                                       SnapsToDevicePixels="True"
                                       UseLayoutRounding="True"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       Visibility="{Binding CurrentPhoto, Converter={StaticResource ValidPathToVisibilityConverter}, FallbackValue=Collapsed}"
                                       Source="{Binding CurrentPhoto, Converter={StaticResource PhotoPathToImageSourceConverter}}">
                                    <Image.Clip>
                                        <RectangleGeometry Rect="0,0,224,284" RadiusX="8" RadiusY="8"/>
                                    </Image.Clip>
                                </Image>
                                
                                <Border BorderBrush="{DynamicResource MaterialDesign.Brush.Secondary}"
                                        BorderThickness="2" CornerRadius="8"
                                        Visibility="{Binding CurrentPhoto, Converter={StaticResource ValidPathToVisibilityConverterReversed}, FallbackValue='Visible'}">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="AccountBoxOutline" Width="96" Height="96" Foreground="{DynamicResource MaterialDesign.Brush.Secondary}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="គ្មានរូបភាព" FontSize="18" Foreground="{DynamicResource MaterialDesign.Brush.Secondary}" HorizontalAlignment="Center" Margin="0 8 0 0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </materialDesign:Card>
                        
                        <Button Margin="0 24 0 0" Command="{Binding UploadPhotoCommand}" Width="200" Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ImageEdit" Margin="0 0 8 0"/>
                                <TextBlock Text="បង្ហោះរូបភាព" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </materialDesign:Card>

        <!-- Footer: navigation -->
        <Border Grid.Row="2" Background="{DynamicResource MaterialDesign.Brush.Surface}" CornerRadius="8" Padding="16" Margin="0 16 0 0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Margin="0,0,8,0" Command="{Binding BackCommand}" ToolTip="Ctrl + Q" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ArrowLeft" Margin="0 0 8 0"/>
                            <TextBlock Text="ត្រលប់ក្រោយ" />
                        </StackPanel>
                    </Button>
                    <Button Margin="0" Command="{Binding NextCommand}" ToolTip="Ctrl + E" Style="{StaticResource MaterialDesignRaisedButton}" Width="120">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="បន្ទាប់" Margin="0 0 8 0"/>
                            <materialDesign:PackIcon Kind="ArrowRight" />
                        </StackPanel>  
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Margin="0" Command="{Binding AddStudentCommand}" ToolTip="Enter" Style="{StaticResource MaterialDesignRaisedButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" Margin="0 0 8 0"/>
                            <TextBlock Text="បញ្ចូលសិស្ស" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
